<%- include('../partials/head') %>
<%- include('../partials/header') %>
<%- include('../partials/page-header', { pageHeaderTitle: 'Página no encontrada' }) %>

<section class="section text-center">
    <div class="container">
        <i class="bi bi-exclamation-triangle" style="font-size:3.5rem; color:var(--accent);"></i>
        <h1 style="font-size:4rem; font-weight:700; margin:16px 0;">404</h1>
        <p class="mb-4">La página que buscas no existe o fue movida.</p>
        <a class="btn-pill btn-pill--solid" href="/">Volver al inicio <i class="fas fa-arrow-right"></i></a>
    </div>
</section>

<%- include('../partials/footer') %>
